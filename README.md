# Task Manager

Full-stack project using Rust (backend) and React + TypeScript (frontend).

## Backend (API) development

See `backend/api/README.md` for platform-specific build instructions, especially Windows devs (MSVC/wsl/MSYS2 requirements).
